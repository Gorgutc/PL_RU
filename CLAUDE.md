# Legacy Claude Pointer

This repository has migrated from Claude Code to Codex.

The canonical agent instructions now live in `AGENTS.md`, with repo-local Codex skills under `plugins/pl-ru-codex/skills/` and plugin metadata under `.agents/plugins/marketplace.json`.

Claude-specific `.claude/` settings, agents, commands, and skills are no longer the source of truth.
