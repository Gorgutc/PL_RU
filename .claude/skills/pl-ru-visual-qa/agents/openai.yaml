interface:
  display_name: 'PL_RU Visual QA'
  short_description: 'Pixel-level visual QA against references'
  default_prompt: 'Use $pl-ru-visual-qa to compare PL_RU UI changes against reference PNGs with Playwright screenshots, pixel diffs, and DOM/CSS metric assertions.'
policy:
  allow_implicit_invocation: true
