# Convenience mirror for Codex sessions working in PL_RU.
# Canonical authority remains:
# verify-frozen.ts > current user request > AGENTS.md = CLAUDE.md > skills (plugins/pl-ru-codex/skills/** = .claude/skills/**) > design references
# AGENTS.md and CLAUDE.md are two equivalent canons; parity guarded by rule A16.

[features]
memories = true

[project]
name = "PL_RU"
repository = "Gorgutc/PL_RU"
branch_prefix = "codex/"
package_manager = "pnpm"
node = "24"

[workflow]
bootstrap_doc = "docs/agent/bootstrap.md"
orchestration_doc = "docs/agent/orchestration.md"
ship_command = "corepack pnpm run codex:ship"
verify_command = "corepack pnpm run verify"

[quality]
fast = "corepack pnpm run quality:fast"
deep = "corepack pnpm run quality:deep"
all = "corepack pnpm run quality:all"
visual = "corepack pnpm run check:visual"

[limits]
read_only_audit_agents = 10
required_read_only_agents = [
  "code_deadwood_auditor",
  "code_quality_guardian",
  "component_reuse_guardian",
  "runtime_behavior_mapper",
  "tech_stack_cartographer",
  "instruction_drift_auditor",
  "quality_tooling_architect",
  "codex_infra_architect",
  "frozen_decisions_guardian",
  "visual_qa_guardian",
]
worker_agents_require_disjoint_write_scopes = true
production_cleanup_requires_separate_pr = true

[read_only_references]
paths = [
  "Blueprints_lib/",
  "Osiris_ref/",
  "plugins/pl-ru-codex/skills/blueprint-design/",
  "plugins/pl-ru-codex/skills/osiris-design/",
]

